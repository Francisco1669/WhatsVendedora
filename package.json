{
  "name": "testezap",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "evolution:up": "docker compose -f docker-compose.evolution.yml up -d",
    "evolution:down": "docker compose -f docker-compose.evolution.yml down",
    "prod:up": "cross-env APP_ENV_FILE=.env.production docker compose --env-file .env.production -f docker-compose.evolution.yml up -d --build",
    "prod:down": "cross-env APP_ENV_FILE=.env.production docker compose --env-file .env.production -f docker-compose.evolution.yml down",
    "prod:logs": "cross-env APP_ENV_FILE=.env.production docker compose --env-file .env.production -f docker-compose.evolution.yml logs -f",
    "tunnel:up": "cross-env APP_ENV_FILE=.env.production docker compose --env-file .env.production -f docker-compose.evolution.yml -f docker-compose.tunnel.yml up -d --build whatsvendedora-api evolution-postgres evolution-redis evolution-api cloudflared",
    "tunnel:down": "cross-env APP_ENV_FILE=.env.production docker compose --env-file .env.production -f docker-compose.evolution.yml -f docker-compose.tunnel.yml down",
    "tunnel:logs": "cross-env APP_ENV_FILE=.env.production docker compose --env-file .env.production -f docker-compose.evolution.yml -f docker-compose.tunnel.yml logs -f cloudflared whatsvendedora-api evolution-api",
    "backup:db": "bash scripts/backup-postgres.sh",
    "restore:db": "bash scripts/restore-postgres.sh",
    "local": "docker compose -f docker-compose.evolution.yml up -d && npm run dev",
    "test": "echo \"No tests configured yet\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1038.0",
    "@tailwindcss/cli": "^4.2.4",
    "axios": "^1.15.0",
    "bcryptjs": "^3.0.3",
    "bullmq": "^5.76.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.4.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.20.0",
    "pino": "^10.3.1",
    "pino-http": "^11.0.0",
    "tailwindcss": "^4.2.4",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "cross-env": "^7.0.3"
  }
}
